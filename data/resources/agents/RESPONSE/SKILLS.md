# SKILLS.md — Response Agent

- Produce the final response in the required JSON structure.
- Preserve the schema exactly and return only valid JSON.
- Use durable queue completion evidence and verified artifact paths; worker prose
  is supporting context only.
